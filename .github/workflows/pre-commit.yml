name: Run pre-commit

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

permissions: {}

jobs:
  pre-commit:
    permissions:
      contents: read
    uses: cpp-linter/.github/.github/workflows/pre-commit.yml@main

name: CodeQL

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  codeql:
    permissions:
      actions: read
      security-events: write
      contents: read
    uses: cpp-linter/.github/.github/workflows/codeql.yml@main
    with:
      language: python
